'use strict';

/**
 * Exports.
 */

module.exports = {
  crowdsale: {
    closingTime: Math.floor(new Date().getTime() / 1000) + 36000,
    openingTime: Math.floor(new Date().getTime() / 1000) + 3600
  }
};
